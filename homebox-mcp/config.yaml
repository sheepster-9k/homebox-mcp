---
name: "Homebox MCP Server"
description: "MCP Server to manage Homebox inventory via AI"
version: "0.2.4"
slug: "homebox-mcp"
url: "https://github.com/oangelo/homebox-mcp"
init: false
arch:
  - amd64
  - aarch64
ports:
  8099/tcp: 8100
ports_description:
  8099/tcp: "MCP Server SSE endpoint"
ingress: true
ingress_stream: true
panel_icon: "mdi:package-variant-closed"
panel_title: "Homebox MCP"
options:
  homebox_url: "http://homeassistant.local:7745"
  homebox_token: ""
  homebox_username: ""
  homebox_password: ""
  mcp_auth_enabled: false
  mcp_auth_token: ""
  log_level: "info"
schema:
  homebox_url: str
  homebox_token: str?
  homebox_username: str?
  homebox_password: password?
  mcp_auth_enabled: bool
  mcp_auth_token: str?
  log_level: list(trace|debug|info|warning|error)
